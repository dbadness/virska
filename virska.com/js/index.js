$(document).ready(function() {

	// make the top nac bar slide with the page
	$('#navBar').css("position", "fixed");
	
	// move the main graphic down so it fits
	$('.spacer:first').css("height", "140px");
	
});